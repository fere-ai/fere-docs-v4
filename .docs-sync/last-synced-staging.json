{
  "all_in_one_staging_sha": "7dceca96911d73c0f0a298a3f261cc22964e0084",
  "synced_date": "2026-09-09",
  "notes": "Run 42 (2026-09-09): diffed staging from the Run 41 SHA (a7fdf52c, 2026-09-08) to current HEAD (7dceca9, 2026-09-09) for webapp/ changes -- a one-day, 45-file diff concentrated in webapp/app/(authenticated)/portfolio/ (history-table.tsx rewrite, main-account-pane.tsx, strategy-account-pane.tsx, venue-lens.tsx, a new wallet-trades.tsx), webapp/app/(authenticated)/components/onboarding-flow/ (a new onboarding-help-menu.tsx step), webapp/lib/hooks/wallet-service/ (use-pnl-history.ts rewrite, a new refresh-selected-wallet.ts), webapp/lib/notifications/holdings-refresh-klasses.ts, webapp/lib/providers/notification-websocket-provider.tsx, webapp/lib/utils/query-keys.ts (+1 line), and webapp/tailwind.config.ts (+1 line).\n\nRe-verified the flag inventory first: confirmed webapp/lib/flags/ has the identical file listing at both SHAs (account-deletion-flags.ts, alpha-engine.ts, alpha-lab.ts, product-surfaces.ts, yield-flags.ts, plus the pre-existing hyperliquid-segment.ts helper) -- no 6th flag introduced, and the new onboarding-help-menu.tsx is mounted unconditionally (no flag/condition around it). Also confirmed via `git diff --stat -- webapp/app` that no new top-level page.tsx (new route) was added in this range.\n\nFound two real gaps, both from this range (not a deeper pre-existing sweep this run -- Run 41 already did that two days ago and nothing in this diff touches strategies.mdx/wallet.mdx/index.mdx/settings.mdx's previously-reconciled areas):\n\n1. MISSING (faq.mdx, General section, wallet-connect-signup onboarding accordion): webapp/app/(authenticated)/components/onboarding-flow/steps/onboarding-help-menu.tsx is a new file now mounted unconditionally in onboarding-chrome.tsx's header, next to the step-progress nav. Its own doc comment: 'Escape hatch for the non-dismissible onboarding modal (no close button, no backdrop/escape dismiss). Without this, a user stuck on a step... has no way out.' Renders a CircleHelp icon -> dropdown with Contact support (opens socialLinks.support) and Sign out. Added one sentence to the existing 'I signed up with a wallet...' FAQ accordion (same precedent as Run 41's chat-sharing addition: small ungated feature -> single FAQ sentence, not a new page) rather than inventing a new page for a single escape-hatch menu.\n\n2. OUTDATED (portfolio.mdx, Main Wallet section): the 'refresh icon... forces an immediate re-fetch of your holdings' sentence undersold the actual scope. webapp/lib/hooks/wallet-service/refresh-selected-wallet.ts (new file) is now wired into both main-account-pane.tsx and strategy-account-pane.tsx's refresh button (replacing the old direct hardRefreshHoldingsV2/hardRefreshAgentHoldings calls, which only touched holdings) -- its doc comment: 'Wallet-head reload: hard Positions + hard Trades + the three open-order lists for this agent. Spinner waits on every GET.' It fires hardRefreshPnlHistory, hardRefreshLimitOrders, hardRefreshPolymarketOpenOrders, and hardRefreshHyperliquidOpenOrders in parallel with the holdings refresh (Promise.allSettled). The visible button/aria-label text is unchanged (still 'Refresh Main holdings' / 'Refresh {name} holdings') -- only the underlying scope changed, so this was a substance fix: reworded to 'forces an immediate re-fetch of your Positions, Trades, and Orders together'.\n\nChecked and found OK (no doc action, confirmed via direct diff/code read rather than assumed):\n- history-table.tsx's 346-line rewrite: columns (Time/Venue/Side/Asset/USD Value/Activity) unchanged; the rewrite only adds a responsive compact-venue-label + tooltip on narrow viewports and drops an unused loadingWhenEmpty prop now that Strategy's Trades tab is a separately-mounted WalletTrades instance. No user-facing column/filter change.\n- wallet-trades.tsx (new): a pure structural extraction of the existing Trades-tab rendering (usePnlHistory fetch + HistoryTable), mounted only while the Trades tab is selected. Docs' existing Trades description (chronological log, Activity tag, infinite scroll) remains accurate as-is.\n- main-account-pane.tsx / strategy-account-pane.tsx: same Positions/Trades/Orders segmented control as before, just refactored to use the new shared WalletTrades component; Strategy pane's Trades tab now fetches directly by agentId instead of filtering Main's shared history feed -- this is actually closer to the docs' existing 'scoped to this strategy's own wallet' claim, so no change needed (and slightly more accurate than before).\n- venue-lens.tsx: pure color-token/shape restyle (active-state border color, square->round dot), no behavioral change to the venue chip filter.\n- query-keys.ts +1 line: adds a `pnlHistoryPrefix` helper key alongside the existing `pnlHistory` key; the existing key's `v2` version tag is unchanged -- not a cache-breaking bump, no doc action per the project's query-key-version convention.\n- notification-websocket-provider.tsx (160-line diff) + holdings-refresh-klasses.ts (+51 lines): internal cache-invalidation policy rework only (three-tier soft/hard/skip refresh mode, per-agent refresh timers) -- does not change any notification text or documented UI behavior.\n- tailwind.config.ts +1 line: adds a content glob for files that moved partly under lib/ -- build config only, zero runtime impact.\n\nFull Run 1-41 history is preserved in this file's git blame/log on this branch and in PR #13's earlier revisions; not re-pasted here to keep this file readable -- see `git log -p -- .docs-sync/last-synced-staging.json` on branch docs-sync/2026-07-13 for the complete run-by-run audit trail."
}
