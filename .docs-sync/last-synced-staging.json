{
  "all_in_one_staging_sha": "661c0e29f3dbc09d6f13163abb206231f3e7a5e5",
  "synced_date": "2026-08-13",
  "notes": "Run 22 (2026-08-13): diffed staging from the Run 21 SHA (a37c046, 2026-08-12) to current HEAD (661c0e2, 2026-08-13). `git log --oneline a37c046..661c0e2` shows 10 commits, of which 3 touch `webapp/`: (1) #2098 'chore: remove home hot picks and deprecate follow-up-queries API' -- removes the 'Hottest Topics' recommendation carousel from the `/app` dashboard showcase and deprecates `GET /chat/follow-up-queries`; (2) #2100 'chore(webapp): remove Hottest Topics from /research' -- the follow-up cut that removes the same carousel from `/research` (the documented chat/research landing page); (3) #2102 the merge commit for #2100. Checked `grep -rniE 'hottest|hot pick|follow-up-queries|hot topic' *.mdx` across every doc file: zero matches -- this carousel was never described in the docs (index.mdx's 'Interface at a Glance' section covers the greeting + chat input only), so its removal from both `/app` and `/research` is not a documentable regression. Classification: OK, no doc change needed. The remaining 7 commits in range are `friday/`-only (Langfuse trace-I/O nesting fixes #2095/#2099, a test-only AI-precompute opt-in #2092, and #2096 'deliver partial v4 write-ups instead of generic failure') -- #2096 is user-facing but is already fully covered by the independently-opened, human-authored companion PR #21 ('docs: note incomplete research answers still show the write-up', branch `docs/pr2096-incomplete-answers-main`, explicitly titled as a companion to all-in-one#2096); verified PR #21's diff describes the correct behavior (timed-out/partially-failed research still surfaces the write-up already produced, with a note that sources may be incomplete) -- no gaps found, so per the 'don't duplicate work already open elsewhere' policy this run makes no additional content edits. Light full sweep: re-verified `webapp/app/.well-known/vercel/flags/route.ts` still registers exactly the same three flags (account-deletion, yield-page, product-surfaces) with unchanged gating logic (`git diff a37c046..661c0e2 -- webapp/lib/flags/` and `-- webapp/app/.well-known/vercel/flags/route.ts` both empty); confirmed no new top-level route/page/layout files were added under `webapp/app/` in this range (`git diff --name-status a37c046..661c0e2 -- 'webapp/app/**/page.tsx' 'webapp/app/**/layout.tsx'` empty). No drift found this run. Only this sync-state file bumped.",
  "flag_mechanism_reverified": "Vercel Flags SDK (webapp/app/.well-known/vercel/flags/route.ts) still registers exactly three flags as of 661c0e2 (2026-08-13, byte-identical to a37c046/2026-08-12): account-deletion (on for everyone), yield-page (gated via isHyperliquidInternalIdentity), and product-surfaces (gated via isProductSurfacesAllowlisted / PRODUCT_SURFACES_ALLOWLIST_EMAILS|WALLETS env, default OFF for everyone -- hides /app, /polymarket, /discover, /market-pulse). STRATEGY_SUB_AGENTS_ENABLED remains a separate backend-only env flag, default off, still excluded."
}
